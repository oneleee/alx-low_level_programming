#include "main.h"
#include <stdio.h>
/**
 * mul - multiplies two numbers
 * @a: the first number to be multiplied
 * @b: the second number to be multiplied
 * main - check the code.
 *
 * Return: Always 0.
 */
int mul(int a, int b)
{
return (a * b);
}
